#!/bin/zsh

#Comando ECHO sirve para mandar "imprimir a consola"
#en terminal ocupamos 'chmod -x nombrearchivo.sh' para asignar que el archivo es ejecutable
#verificamos con  'ls -al nombrearchivo.sh' que tenga -rwxr-xr-x que es un ejecutable
#con './nombrearchivo' ejecutamos el archivo

echo "Hola Mundo"
